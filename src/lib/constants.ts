/**
 * lib/constants.ts
 *
 * Shared constants used across both client and server code.
 * No server-only dependencies — safe to import anywhere.
 */

/** The earliest date available in NASA's APOD archive. */
export const APOD_START_DATE = '1995-06-16'
